// Metric event keys. Must match the metrics created by the bootstrap script
// and attached to the experiment in LaunchDarkly.
export const METRICS = {
  // Conversion event: user clicked the hero call-to-action.
  CTA_CLICK: 'hero-cta-click',
};
